# armbian-s905l3a-rt-kernel
# S905L3A Armbian 实时内核编译
## 使用方法
1. 点击仓库顶部的 **Actions** 标签；
2. 选择左侧 **Build S905L3A RT Kernel**；
3. 点击 **Run workflow**，填写内核版本（默认`6.12.85`）、类型（默认`rt`）；
4. 等待30-60分钟编译完成，在 **Artifacts** 栏目下载`s905l3a-rt-kernel.zip`即可。
